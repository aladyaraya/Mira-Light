# Mira Light 展位场景表

## 文档目的

注意：

当前仓库里的动作实现与排练审计，已经优先以 [`Mira Light 展位交互方案2.pdf`](/Users/Zhuanz/Documents/Github/Mira-Light/docs/Mira%20Light%20展位交互方案2.pdf) 为准。

本文件保留为场景总表和导演层阅读材料，但其中部分动作描述仍带有早期稿件口径，不应直接视为当前代码真值。

当前实现状态、已修问题和剩余缺口请优先对照：

- [`docs/mira-light-pdf2-implementation-audit.md`](/Users/Zhuanz/Documents/Github/Mira-Light/docs/mira-light-pdf2-implementation-audit.md)
- [`docs/mira-light-pdf2-engineering-handoff.md`](/Users/Zhuanz/Documents/Github/Mira-Light/docs/mira-light-pdf2-engineering-handoff.md)

这份文档把 [`Mira Light 展位交互方案.pdf`](/Users/Zhuanz/Documents/Github/Mira-Light/docs/Mira%20Light%20展位交互方案.pdf) 中的自然语言场景，整理成可以直接用于：

- 展位排练
- 主持人口播
- 控制脚本编排
- 设备验收
- 故障回退

每个场景都统一拆成：

- 场景目标
- 触发条件
- 主持人口播
- 动作序列
- 灯光设计
- 依赖能力
- 触发方式
- 失败回退

## 使用方法

这份场景表最适合搭配三类系统一起用：

1. 主持人手卡  
2. 展位控制台  
3. 单片机导演脚本

当前更符合实际的触发方式只有两种：

1. `OpenClaw` 触发  
思路是让 `OpenClaw` 最终调用电脑终端命令，执行 [`scripts/booth_controller.py`](/Users/Zhuanz/Documents/Github/Mira-Light/scripts/booth_controller.py)。

2. 电脑终端直接触发  
例如：

```bash
python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 wake_up
python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 celebrate
python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 farewell
```

如果后面真的做网页控制台、物理按钮或快捷键，那应该视作后续增强，而不是当前默认前提。

## 场景 1：起床

| 项目 | 内容 |
| --- | --- |
| 场景名 | 起床 |
| 目标情绪 | 苏醒、欢迎、轻微笨拙感 |
| 观众感受 | 它不是机械启动，而是像一个刚醒的小动物 |
| 触发条件 | 检测到人靠近约 `1.5m`，或主持人决定开始开场场景 |
| 主持人口播 | “当 Mira 感觉到有人靠近，它不会立刻机械转头，而是像刚睡醒一样慢慢睁眼、抖一抖、伸个懒腰。” |
| 动作序列 | 微光亮起 -> 灯臂缓缓升起 -> 中途抖两下 -> 灯头抬起 -> `stretch` -> 回到正常姿态 |
| 灯光设计 | 先暖色微光，再过渡到柔和常亮 |
| 建议代码原语 | `light_dim_warm`, `arm_raise_slow`, `micro_shiver`, `act_stretch`, `pose_normal` |
| 依赖能力 | 人体接近检测；如果还没接传感器，则由 Claw 或终端直接触发 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 wake_up`；终端：直接运行同一命令 |
| 失败回退 | 若传感器失效，直接用终端命令或 Claw 命令触发固定动作序列 |
| 验收标准 | 动作总时长约 `4~6s`；视觉效果是“醒来”，不是“突然弹起” |

## 场景 2：好奇你是谁

| 项目 | 内容 |
| --- | --- |
| 场景名 | 好奇你是谁 |
| 目标情绪 | 好奇、怯生生、礼貌 |
| 观众感受 | 它会观察你、试探你，而不是冷冰冰对准你 |
| 触发条件 | 评委到桌前，处于正前方区域；或主持人决定进入“好奇观察”段落 |
| 主持人口播 | “Mira 会先试探着看你，而不是直接盯着你。这种停顿和歪头，会让人立刻觉得它是活的。” |
| 动作序列 | 朝目标半转 -> 停顿 -> 继续转向 -> 左歪头 -> 停 `2s` -> 轻点头 |
| 灯光设计 | 柔和暖白，不抢动作戏份 |
| 建议代码原语 | `pose_curious_half_turn`, `turn_to_target_full`, `pose_head_tilt_left`, `act_nod` |
| 依赖能力 | 目标方向估计，或预设左/中/右方向 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 curious_observe`；终端：直接运行同一命令 |
| 失败回退 | 如果目标定位失败，固定朝主持人预设方向执行 |
| 验收标准 | 必须有“半转 + 停顿 + 歪头 + 点头”四段节奏 |

## 场景 3：摸一摸

| 项目 | 内容 |
| --- | --- |
| 场景名 | 摸一摸 |
| 目标情绪 | 亲近、依赖、舒服 |
| 观众感受 | 它会主动靠近你的手，而不是被动挨摸 |
| 触发条件 | 评委把手伸向灯头；或主持人口头引导后进入该场景 |
| 主持人口播 | “你可以摸摸它。它会主动靠过来，不只是响应动作，而是在表达亲近。” |
| 动作序列 | 灯头前探 -> 靠向手掌 -> 小幅左右蹭动 -> 手移开后追一下 -> 回正 |
| 灯光设计 | 切到最暖色温，亮度略升 |
| 建议代码原语 | `pose_extend`, `rub_motion`, `follow_target_once`, `pose_normal`, `light_warmest` |
| 依赖能力 | 手部位置检测、触摸传感器；如果没有，则由 Claw 或终端直接切场景 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 touch_affection`；终端：直接运行同一命令 |
| 失败回退 | 如果检测不到手，直接演“前探 + 左右轻蹭”固定版本 |
| 验收标准 | 动作应小而亲密，不能像攻击或撞击 |

## 场景 4：卖萌

| 项目 | 内容 |
| --- | --- |
| 场景名 | 卖萌 |
| 目标情绪 | 可爱、呆萌、胆小但好奇 |
| 观众感受 | 它像小狗一样在研究你 |
| 触发条件 | 主持人切场景，或在“好奇”之后自然衔接 |
| 主持人口播 | “它会像小狗一样歪头研究你，有时还会探头一下又缩回去，像在确认你是不是安全。” |
| 动作序列版本 A | 左歪头 -> 停 `1s` -> 右歪头 -> 停 `1s` |
| 动作序列版本 B | 前探 -> 停顿 -> 快速缩回 -> 再前探 |
| 灯光设计 | 暖白常亮 |
| 建议代码原语 | `pose_head_tilt_left`, `pose_head_tilt_right`, `pose_extend`, `pose_retract` |
| 依赖能力 | 无强依赖，可完全通过 Claw 或终端触发 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 cute_probe`；终端：直接运行同一命令 |
| 失败回退 | 只保留左右歪头版，降低复杂度 |
| 验收标准 | 节奏要慢，动作幅度不能太大，否则会丢掉“萌感” |

## 场景 5：发呆

| 项目 | 内容 |
| --- | --- |
| 场景名 | 发呆 |
| 目标情绪 | 走神、松弛、像有自己的小心思 |
| 观众感受 | 它不是只会等待命令，而像在自己想事情 |
| 触发条件 | 长时间无互动，或主持人切入展示“日常感” |
| 主持人口播 | “它不会一直表演，它也会像人一样走神、发呆，甚至像打瞌睡一样。” |
| 动作序列版本 A | 慢慢抬头 -> 看向随机方向 -> 停 `3~4s` -> 快速回神 |
| 动作序列版本 B | 头慢慢越来越低 -> 接近桌面 -> 突然弹回 |
| 灯光设计 | 保持当前工作光，或略降亮度 |
| 建议代码原语 | `look_random_direction`, `hold`, `snap_back_to_normal`, `sleepy_drop_and_bounce` |
| 依赖能力 | 计时器即可 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 daydream`；终端：直接运行同一命令 |
| 失败回退 | 用版本 A 固定角度模拟，不依赖随机目标 |
| 验收标准 | 重点是“停住那几秒”，要有空白感 |

## 场景 6：久坐检测蹭蹭

| 项目 | 内容 |
| --- | --- |
| 场景名 | 久坐检测：蹭蹭 |
| 目标情绪 | 提醒，但提醒方式可爱不冒犯 |
| 观众感受 | 它不是发警报，而是像宠物一样提醒你该起来了 |
| 触发条件 | 久坐检测触发，或主持人引导“假装你已经坐了一小时” |
| 主持人口播 | “如果你坐太久，它不会直接警报，而是会蹭蹭你，像在说：该起来动一动了。” |
| 动作序列 | 底座转向人 -> 灯臂升高 -> 先回收再前顶，连续 `3` 次 -> 双点头 -> 若被拒绝则轻摇头 |
| 灯光设计 | 柔和工作光，不喧宾夺主 |
| 建议代码原语 | `turn_to_target_full`, `pawing_bump`, `act_nod`, `act_shake` |
| 依赖能力 | 久坐检测逻辑；如果还没接入，则由 Claw 或终端直接触发 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 standup_reminder`；终端：直接运行同一命令 |
| 失败回退 | 固定朝前执行 `蹭蹭 + 点头` |
| 验收标准 | “蹭蹭”必须是三次有节奏前顶，不是乱抖 |

## 场景 7：追踪

| 项目 | 内容 |
| --- | --- |
| 场景名 | 追踪（展示感知能力） |
| 目标情绪 | 专注、跟随、聪明 |
| 观众感受 | 它真的看得见，不只是预编程动作 |
| 触发条件 | 评委在桌面移动书本，或主持人决定进入追踪演示 |
| 主持人口播 | “你试着移动这本书。它会一直跟着书看，光也会跟着书走。” |
| 动作序列 | 锁定目标 -> 持续平滑跟随 -> 目标停止则停 -> 再次移动则继续跟 |
| 灯光设计 | 光照中心尽量随目标移动，亮度保持功能性 |
| 建议代码原语 | `track_target_loop`, `gentle_follow`, `beam_focus_update` |
| 依赖能力 | 摄像头、视觉目标识别、目标坐标到关节角度映射 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 track_target`；终端：直接运行同一命令 |
| 失败回退 | 若视觉失败，改为主持人从终端触发固定版追踪说明，或用滑杆模拟目标方向 |
| 验收标准 | 目标移动时灯头必须连续跟，而不是一步一跳 |

## 场景 8：跳舞模式

| 项目 | 内容 |
| --- | --- |
| 场景名 | 模拟拿到 offer：跳舞模式 |
| 目标情绪 | 兴奋、庆祝、情绪爆发 |
| 观众感受 | 这个灯不仅会工作，还会表达开心 |
| 触发条件 | 演示电脑打开 offer 页面，主持人决定进入庆祝段落 |
| 主持人口播 | “当它收到一个超级开心的消息时，它会像真的高兴一样跳起来。” |
| 动作序列 | 切彩虹灯 -> 播放音乐 -> `dance` -> 辅助摇摆 -> 慢慢减速 -> 回正 -> 轻甩头 |
| 灯光设计 | `rainbow_cycle` 或 disco 感闪烁 |
| 建议代码原语 | `light_disco`, `act_dance`, `decelerate_to_normal`, `play_music` |
| 依赖能力 | 本地音频播放器、假邮件页面；场景本身由 Claw 或终端触发 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 celebrate`；终端：直接运行同一命令 |
| 失败回退 | 如果音乐失败，仍保留灯光 + `dance` 动作 |
| 验收标准 | 必须有明显“开心过头”的节奏对比，结束后要慢慢收回来 |

## 场景 9：挥手送别

| 项目 | 内容 |
| --- | --- |
| 场景名 | 评委走了，挥挥手 |
| 目标情绪 | 目送、不舍、礼貌告别 |
| 观众感受 | 它在送你走，而不是突然停止 |
| 触发条件 | 评委准备离开，或主持人决定进入结束段落 |
| 主持人口播 | “当你离开时，它会目送你，还会像小狗一样摆摆头和你说再见。” |
| 动作序列 | 跟随离开方向看过去 -> 快速左右摆两下模拟挥手 -> 微微低头 |
| 灯光设计 | 亮度略降，情绪变柔 |
| 建议代码原语 | `follow_departing_direction`, `act_wave`, `head_lower_gently` |
| 依赖能力 | 离场方向估计；如果没有，则由 Claw 或终端直接触发固定告别版本 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 farewell`；终端：直接运行同一命令 |
| 失败回退 | 直接执行固定 `wave + 低头` |
| 验收标准 | 重点是“先目送，再挥手”，而不是上来就 wave |

## 场景 10：睡觉

| 项目 | 内容 |
| --- | --- |
| 场景名 | 睡觉 |
| 目标情绪 | 收场、回归安静、等待下一位 |
| 观众感受 | 演示闭环完整，不是突然熄火 |
| 触发条件 | 评委走远，或送别场景结束后自动进入 |
| 主持人口播 | “当人离开后，它会慢慢收回自己，回到休息状态，等下一个人来。” |
| 动作序列 | 低头 -> 灯臂缓缓降下 -> `stretch` 的收尾版 -> 回到蜷缩姿态 -> 微光或熄灭 |
| 灯光设计 | 亮度逐步降低到微光 |
| 建议代码原语 | `act_stretch`, `pose_sleep`, `light_sleep_fade` |
| 依赖能力 | 无强依赖 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 sleep`；终端：直接运行同一命令 |
| 失败回退 | 直接执行 `reset + 灯光调暗` |
| 验收标准 | 从“在场”回到“休息”必须是平滑过渡 |

## 补充场景 A：叹气检测

| 项目 | 内容 |
| --- | --- |
| 场景名 | 叹气检测 |
| 目标情绪 | 理解、安慰 |
| 观众感受 | 它感知到了你的状态变化 |
| 触发条件 | 检测到明显叹气声，或主持人决定演示“叹气检测” |
| 主持人口播 | “你对着它叹一口气，它就会像听懂了一样看你一下，光也会变暖。” |
| 动作序列 | 歪头 -> 轻微低头 -> 暖光呼吸 |
| 灯光设计 | 暖色、低频呼吸 |
| 建议代码原语 | `pose_head_tilt_left`, `head_lower_gently`, `light_breath_warm` |
| 依赖能力 | 麦克风 + 音频模式识别 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 sigh_demo`；终端：直接运行同一命令 |
| 失败回退 | 直接从终端或 Claw 触发固定安慰反应 |
| 优先级 | 二期 |

## 补充场景 B：多人反应

| 项目 | 内容 |
| --- | --- |
| 场景名 | 多人反应 |
| 目标情绪 | 左右为难、像小动物不知道该看谁 |
| 观众感受 | 现场更有趣，也更像活物 |
| 触发条件 | 同时识别到两个目标 |
| 主持人口播 | “如果同时有两个人，它会短暂纠结，不知道该先看谁，最后才选定一个。” |
| 动作序列 | 左看 -> 右看 -> 左看 -> 停顿 -> 面向主目标 |
| 灯光设计 | 常规暖白 |
| 建议代码原语 | `scan_targets`, `select_primary_target`, `face_target` |
| 依赖能力 | 多目标检测 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 multi_person_demo`；终端：直接运行同一命令 |
| 失败回退 | 用固定左右摇摆代替真实多目标输入 |
| 优先级 | 二期 |

## 补充场景 C：语音理解

| 项目 | 内容 |
| --- | --- |
| 场景名 | 语音理解 |
| 目标情绪 | 被听懂、被接住 |
| 观众感受 | 它不需要说话，也能表达“我听懂了” |
| 触发条件 | 识别到“今天好累啊”等情绪语句 |
| 主持人口播 | “你只要随便说一句‘今天好累啊’，它就会用动作和灯光告诉你：它听懂了。” |
| 动作序列 | 轻低头 -> 暖色呼吸 -> 缓慢点头或静止陪伴 |
| 灯光设计 | 暖色缓慢呼吸 |
| 建议代码原语 | `speech_emotion_detected`, `head_lower_gently`, `light_breath_warm`, `act_nod` |
| 依赖能力 | 语音识别 + 情绪分类 |
| 触发方式 | `OpenClaw`：运行 `python3 scripts/booth_controller.py --base-url tcp://192.168.31.10:9527 voice_demo_tired`；终端：直接运行同一命令 |
| 失败回退 | 直接从终端或 Claw 触发“疲惫安慰”动画 |
| 优先级 | 二期 |

## 补充场景 D：鼓励 / 批评反馈

| 项目 | 内容 |
| --- | --- |
| 场景名 | 夸奖与批评反馈 |
| 目标情绪 | 对外部评价作出人格化反应 |
| 观众感受 | 它像有“情绪偏好” |
| 触发条件 | 识别到夸奖或负面评价 |
| 主持人口播 | “你夸它，它会开心；你说它不好，它也会摇头表示委屈。” |
| 动作序列（夸奖） | 轻快上下摆动 |
| 动作序列（批评） | 摇头 -> 灯光略暗 |
| 灯光设计 | 夸奖时更亮更暖，批评时略暗 |
| 建议代码原语 | `act_nod`, `act_shake`, `light_positive`, `light_negative` |
| 依赖能力 | 语义分类 |
| 触发方式 | `OpenClaw`：后续可触发夸奖/批评场景命令；终端：直接运行对应场景命令 |
| 失败回退 | 直接从终端或 Claw 触发，无需语义识别 |
| 优先级 | 二期 |

## 建议现场优先使用的 MVP 场景

如果现场时间和稳定性有限，建议只保留下面 6 个作为必演版本：

| 优先级 | 场景 | 原因 |
| --- | --- | --- |
| P0 | 起床 | 开场最抓人 |
| P0 | 好奇你是谁 | 最能体现“活物感” |
| P0 | 摸一摸 | 最有情感连接 |
| P0 | 追踪 | 最能证明“它看得见” |
| P0 | 跳舞模式 | 最有展位爆点 |
| P0 | 挥手 + 睡觉 | 演示闭环完整 |

其余场景可作为加分项或备选。

## 建议控制台字段

为了让这份场景表真正能用于展位执行，控制台最好至少显示：

- 当前场景名
- 当前状态
- 当前目标方向
- 当前动作是否播放中
- 当前灯光模式
- 当前 4 个关节角度
- 当前触发来源

这里的“当前触发来源”建议统一只记录两类：

- `openclaw`
- `terminal`

## 当前阶段最重要的结论

如果说 [`Mira Light 展位交互方案.pdf`](/Users/Zhuanz/Documents/Github/Mira-Light/docs/Mira%20Light%20展位交互方案.pdf) 是“创意故事板”，那么这份文档就是“排练版导演脚本表”。

你后面写代码、做控制台、准备主持人口播、做现场彩排，都应该以这份场景表为中间层，而不是直接面对 PDF 原文。
